{
  "name": "@extend-ai/react-docx-wasm",
  "private": true,
  "version": "0.1.0",
  "description": "Rust/WASM DOCX parser for react-docx",
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": ["dist", "pkg"],
  "scripts": {
    "build:wasm": "wasm-pack build ../../crates/docx-wasm --target web --out-dir $INIT_CWD/pkg && node ./scripts/inline-wasm.mjs && mkdir -p src/generated && cp pkg/docx_wasm.js pkg/docx_wasm.d.ts pkg/docx_wasm_bg.wasm.d.ts src/generated/",
    "build:js": "tsup src/index.ts --format esm,cjs --dts --sourcemap --clean",
    "build": "pnpm run build:wasm && pnpm run build:js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist pkg"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
